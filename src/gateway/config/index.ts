// Environment configuration
export { env } from './env'

// Routing configuration
export {
  parentResources,
  validParentResources,
  scopedEndpoints,
  validKindsPlural,
  SCOPED_RESOURCE_PATTERN,
  availableEndpoints,
} from './routes'
