export { isHealthCheck, handleHealthCheck, isReadinessCheck, handleReadinessCheck } from './health'

export { isRootGraphQL, isScopedGraphQL, isGraphQLEndpoint, handleGraphQL } from './graphql'

export { isMetrics, handleMetrics } from './metrics'
